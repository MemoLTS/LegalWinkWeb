server:
    port: 8081
    servlet:
        context-path: /api/v1/usuarios

spring:
    application:
        name: servicio-usuarios
    datasource:
        url: jdbc:postgresql://localhost:5432/db_usuarios
        username: postgres
        password: tu_password/media/Compartido/Clases/Application
